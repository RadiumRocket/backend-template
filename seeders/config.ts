export default {
  remove: true,
  create: true,
  // -- FIREBASE --
  firebaseUsers: {
    remove: true,
    create: true,
  },
  // -- MONGO --
  users: {
    remove: true,
    create: true,
  },
};
